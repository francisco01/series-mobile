import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const SeriesPage =({ series }) => (
    <View>
        <Text>Essa é a tela de series</Text>
    </View>
);

const styles = StyleSheet.create({

})

export default SeriesPage;